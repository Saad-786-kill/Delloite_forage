# Delloite_forage
This project standardizes machine telemetry data from two different JSON formats into a single unified structure. Using Python, it extracts, cleans, and transforms flat and nested data, splits location fields, and converts timestamps to milliseconds, enabling consistent and efficient data analysis.
